{
  "name": "@auntie-marlenes/constants",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "check-types": "tsc --noEmit",
    "lint": "echo 'skip: no local ESLint flat config (TODO: add eslint.config.mjs or extend shared monorepo config)'"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "~5.9.2"
  },
  "private": true
}
